# ai-agent-assistant
AI Agent Assistant is a tool-based AI system that answers questions using multiple capabilities including PDF document retrieval (RAG), web search, and mathematical reasoning. It uses a FastAPI backend and Streamlit interface, with a vector database for document understanding and logging for tool usage.
